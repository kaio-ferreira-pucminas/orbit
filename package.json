{
  "name": "orbit",
  "version": "1.0.0",
  "private": true,
  "description": "Orbit — serviço único de deploy: o server.js serve a API, o frontend estático (src/) e os uploads.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/scripts/backend/server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "json-server": "^0.17.4",
    "jsonwebtoken": "^9.0.3",
    "resend": "^6.12.3"
  }
}
